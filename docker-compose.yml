services:
  engine:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  dashboard:
    build: .
    command: python dashboard/app.py
    env_file: .env
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
